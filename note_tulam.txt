frontend
nextjs, typescript, heroicons, react-toastify, yup, hookform, tanstack, react-hook-form, jwt-decode, js-cookie
npm run dev
trang chủ bán hàng
http://localhost:3000/
trang chủ admin
http://localhost:3000/admin



backend 
nodejs, expressjs, path, morgan, cookieParser, createError, bcryptjs, cors, jsonwebtoken, mongoose, multer, slugify
npm run dev
http://localhost:5000/api/products
admin@example.com
123456
admin_con1@example.com
123456
các chức năng làm thêm
sản phẩm mới, sản phẩm bán chạy



git :
tritxps28458@fpt.edu.vn
@Xuantri1998
https://github.com/TuiTenXT/tulam
git pull origin main                  # Luôn cập nhật trước khi làm (có thể bỏ nếu là dự án cá nhân)
git checkout -b feature/abc           # Tạo nhánh mới
git add .  
git commit -m "..."      # Commit thay đổi
git push origin feature/abc           # Push lên GitHub
Tạo Pull Request bằng đường link trong terminal
Review, sửa code nếu cần
Merge vào main
git checkout main        # Đồng bộ máy bạn
git pull origin main 
git branch -d abc 
git branch


Màu nền chính (background): #EBFCFF – Màu xanh nhạt dịu nhẹ, thoáng.
Màu nền phụ (section sáng): #CAF0F8 – Dùng cho block, card nhẹ.
Header / Footer: #03045E – Xanh navy đậm, mạnh mẽ – tạo thương hiệu.
Màu chủ đạo (primary button / CTA): #0077B6 – Màu xanh nổi bật để thúc đẩy hành động.
Hover button chính: #00B4D8 – Tươi sáng, hiệu ứng hover bắt mắt.
Accent border / separator: #EFEDCE – Màu vàng kem nhẹ, tạo cảm giác cao cấp.
Tiêu đề văn bản: #0F214D – Đậm, rõ ràng, sang trọng.
Văn bản phụ: #0B5FB0 – Text hỗ trợ – dễ đọc nhưng không lấn át.
Link hover / icon hover: #2BA3EC – Nhẹ nhàng, tinh tế, dễ nhìn.



tất cả ảnh (product, category,...) đều lưu chung 1 file img, sau chỉ cần lấy đường dẫn up lên, không cần phải chia riêng img/catgeory, img/product,...
làm theo kiểu backend, frontend là chuẩn vì sau này khi up lên host sẽ up 2 cổng khác nhau cho 2 folder
trong 
hình ảnh cái nào dính đến cơ sở dữ liệu (mongodb) thì lưu trong backend, cái nào dính đến template (slider banner,...) thì lưu trong frontend


*những thứ cần hỏi











